# iso - Isolated thread manager configuration
# Copy this file to 'config' and customize for your setup

# Main seer repository path (absolute path required)
SEER_REPO_PATH="/Users/pika/Projects/seer"

# Optional: Frontend repository path (for automatic .env updates)
# SEER_FRONTEND_PATH="/Users/pika/Projects/seer-frontend"

# Optional: Multiple repos (uncomment and add paths as needed)
# SEER_REPO_PATH_2="/path/to/another/seer/clone"

# Port allocation scheme (not configurable):
# Backend API: 3000 + thread_id (3001, 3002, ...)
# Frontend:    4000 + thread_id (4001, 4002, ...)
# Postgres/Redis: Internal Docker network only

# Optional: Max threads (default 10, range 1-10)
# MAX_THREADS=10
